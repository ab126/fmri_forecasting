# TODO: load the cross-validated models and run them on the test set, comparing to the baselines.

# Test should be done on holdout subjects on the cross_validation.py
# train_items, holdout_items = split_by_subject( # 
#        dataset,
#        test_ratio=args.test_ratio,
#        random_state=args.random_state,
#        verbose=True,
#    )


